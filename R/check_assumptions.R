#' check_assumptions function
#' checking main assumptions of statistical tests or reporting the results of the implicit checks of R functions
#' @param df input data
#' @param assumption the name of the assumption of the test
#' @param test_name the name of the statistical test
#' @param model_name the name of the statistical model
#' @import tidyverse
#' @import magrittr
#' @export
#' @examples check_assumptions_test(df = ggplot2::diamonds, assumption = aa, test_name= tt)
#'
#' this is still under construction
#' @export
#'

#check_assumptions <- function(df, model_name, ...) {
#  #--------------
#  }

check_assumptions_test <- function(df, test_name, assumption) {
 #--------------
  print("the test assmption to be checked")
}


check_assumptions_model <- function(model_name, assumption) {
  #---------------
  print("the model assmption to be checked")
}
